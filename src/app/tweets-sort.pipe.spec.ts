import { TweetsSortPipe } from './tweets-sort.pipe';

describe('TweetsSortPipe', () => {
  it('create an instance', () => {
    const pipe = new TweetsSortPipe();
    expect(pipe).toBeTruthy();
  });
});
